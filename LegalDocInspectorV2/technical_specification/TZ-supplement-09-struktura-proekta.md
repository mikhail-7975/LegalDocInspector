# Дополнение к разделу 9. Целевая структура проекта — Вопросы

> Вопросы, на которые необходимо ответить для составления однозначного ТЗ.

---

## Монорепозиторий

### Q-9.1. Инструмент управления монорепозиторием

Какой инструмент использовать для управления монорепозиторием?
- **Nx** (хорошо для JS/TS-тяжёлых проектов)?
- **Turborepo** (быстрый, простой)?
- **Lerna** (классический, менее активно развивается)?
- **Just Docker Compose** (без специального инструмента для монорепо)?
- **Bazel** (для смешанных стеков)?

Или простая структура каталогов без специального инструмента, с отдельными Makefile/scripts?

### Q-9.2. Единый язык или polyglot

Все сервисы на одном языке (например, Python или Node.js) или допускается использование разных языков для разных сервисов? Например:
- `ocr-pipeline` на Python (ML-библиотеки)
- `bff-orchestrator` на Node.js (Fastify/NestJS)
- `calculation-service` на Go (производительность)

Или стремиться к единому стеку для упрощения поддержки?

---

## Фронтенд (apps/web)

### Q-9.3. Фреймворк фронтенда

- React / Next.js?
- Vue / Nuxt?
- Angular?
- Svelte / SvelteKit?

### Q-9.4. UI-библиотека / дизайн-система

- Готовая библиотека (Ant Design, Material UI, Chakra UI, Vuetify)?
- Собственный дизайн с нуля (Tailwind CSS + headless компоненты)?
- Есть ли макеты/прототипы (Figma, Sketch)?
- Нужен ли дизайнер, или разработчик сам определяет UI?

### Q-9.5. State management фронтенда

Для хранения состояния формы с множеством полей, комплектов, ошибок:
- Redux / Zustand / Jotai (React)?
- Pinia (Vue)?
- Или достаточно встроенных средств фреймворка?

---

## Сервисы (services/*)

### Q-9.6. Dockerfile — базовые образы

Какие базовые Docker-образы использовать?
- Для Python-сервисов: `python:3.12-slim` / `python:3.12-alpine`?
- Для Node.js-сервисов: `node:20-slim` / `node:20-alpine`?
- Есть ли требования к корпоративному Docker-реестру?

### Q-9.7. Конфигурация сервисов

Как передаётся конфигурация?
- Переменные окружения?
- Файлы конфигурации (`.env`, `config.yaml`)?
- Секреты через Docker secrets / Kubernetes secrets / Vault?

### Q-9.8. Версионирование сервисов

Как версионируются отдельные сервисы?
- Единая версия для всего проекта (совпадает с тегом git)?
- Независимая версия каждого сервиса (semver)?

---

## Пакеты (packages/*)

### Q-9.9. Общие типы (shared-types)

- Какой язык для общих типов: TypeScript / Python (Pydantic models) / protobuf / JSON Schema?
- Если polyglot-проект — нужна ли кодогенерация типов из единого источника (например, из OpenAPI или protobuf)?

### Q-9.10. debt-certificate-parser

- Где находится код готового парсера — в этом монорепозитории или внешняя зависимость (npm/pypi пакет)?
- Нужна ли обёртка с типизацией и валидацией?

---

## Инфраструктура (infra/)

### Q-9.11. Docker Compose — профили

Нужны ли профили Docker Compose для разных сценариев?
- `development` — с hot-reload, debug-портами
- `testing` — с тест-фикстурами
- `production-like` — максимально приближённый к проду

### Q-9.12. CI/CD

- Какая CI/CD система: GitHub Actions, GitLab CI, Jenkins, другая?
- Какие этапы пайплайна: lint → test → build → push image → deploy?
- Нужны ли автоматические деплойменты (CD) или только сборка (CI)?
- Куда пушить Docker-образы (Docker Hub, GitHub Container Registry, корпоративный реестр)?

### Q-9.13. Тестирование

Какой объём тестов требуется?
- Unit-тесты: обязательны для каждого сервиса?
- Integration-тесты: нужны ли тесты взаимодействия между сервисами?
- E2E-тесты: нужны ли Playwright/Cypress тесты для полного пользовательского сценария?
- Фреймворки для тестирования (Jest, pytest, Vitest)?
- Требуемый уровень покрытия кода (coverage)?

### Q-9.14. Данные для разработки и тестирования

- Есть ли тестовые документы (примеры PDF-договоров, претензий, Excel-справок)?
- Если нет — кто их предоставит?
- Можно ли использовать синтетические (сгенерированные) документы для тестирования?
- Нужны ли fixtures с предзаполненными данными для демо?

---

## Документация (docs/)

### Q-9.15. Уровень документации

Какая документация требуется помимо ТЗ?
- API-документация (Swagger UI из OpenAPI)?
- Архитектурные диаграммы (ADR — Architecture Decision Records)?
- Руководство по развёртыванию (README с инструкцией)?
- Руководство пользователя?
- Инструкция по обновлению шаблонов Word?
